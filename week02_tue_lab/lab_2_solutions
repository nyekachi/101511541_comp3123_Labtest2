console.log("lab_2_solutions")
//Excercise 1

const greeter = (myArray, counter) => {
    const greetText = 'Hello';
    for( const name of myArray){
        console.log(`${greetText}, ${name}`);
    
    }
};
greeter(['Randy Savage','Ric Flair','Hulk Hogan'], 3);

//Excercise 2
const capitalize = (str) => {
    if (typeof str !== 'string' || str.length === 0) return '';
    const first = str.charAt(0);
    const rest = str.slice(1);
    return first.toUpperCase() + rest.toLowerCase();
};
console.log(capitalize('fooBar')); 
console.log(capitalize('nodeJS'));

 //Excercise 3
 //using the same capitalize function from excercise 2
const colors = ['red', 'green', 'blue'];
    const capitalizedColors = colors.map(capitalize);
    console.log(capitalizedColors); // Output: ['Red', 'Green', 'Blue']

//Excercise 4
  var values = [1, 60, 34, 30, 20, 5];
  var filteredValues = values.filter( value => value< 20 );
    console.log(filteredValues); // Output: [1, 5]

//Excercise 5
    var array = [1, 2, 3, 4];
    const calculateSum = array.reduce((accumulator, currentValue) => accumulator + currentValue, 0);
    const calculateProduct = array.reduce((accumulator, currentValue) => accumulator * currentValue, 1);
    console.log(calculateSum); // Output: 10
    console.log(calculateProduct); // Output: 24

//Excercise 6
//Superclass
     class Car{
        constructor(model, year){
            this.model = model;
            this.year = year;
        }
        details(){
            return `Model: ${this.model}, Year: ${this.year}`;
        }
     }
//Subclass
       class Sedan extends Car{
        constructor(model, year, balance){
            super(model, year);
            this.balance = balance;
        }
        details(){
            return `${super.details()}, Balance: $${this.balance.toFixed(2)}`;
        }
    }
    //Testing the Car
    const car2 = new Car('Pontiac Firebird', 1976);
    console.log(car2.details());

    //Testing the Sedan
    const sedan = new Sedan('Volvo SD', 2018, 30000);
    console.log(sedan.details());